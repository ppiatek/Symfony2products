{% extends "PpiatekOrderBundle::layout.html.twig" %}

{% block title "Raport" %}

{% block table %}
  {% if clients %}
    <table>
      <tr>
        <th>Imię</th>
        <th>Nazwisko</th>
      </tr>
      {% for client in clients %}
      <tr>
        <td>{{client.imie}}</td>
        <td>{{client.nazwisko}}</td>
      </tr>
      {% endfor %}
    </table>
    {% if countPages > 1 %}
        <div class="pagination">
            
            {% for i in 1..countPages %}
                <span>{{i}}</span>
            {% endfor %}
        </div>
    {% endif %}
    
  {% endif %}
{% endblock %}
